# AGENTS

> 本目录是通用配置的入口。需要使用其中的规则或技能时，由用户明确发起完整读取。

## 使用约定

完整读取时，AI 从当前配置根目录递归发现实际存在的通用配置；规则和技能按任务需要使用。读取不依赖固定文件清单。

## 目录职责

- `rules/`：保存默认通用规则。
- `skills/`：保存默认可复用任务流程。
- `extensions/rules/`：保存已启用的扩展规则。
- `extensions/skills/`：保存已启用的扩展任务流程。
